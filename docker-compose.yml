version: '3.8'
services:
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - mongodb-data:/data/db

  myapp:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - mongodb

volumes:
  mongodb-data: